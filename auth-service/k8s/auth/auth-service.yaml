apiVersion: v1
kind: Service

metadata:
  name: auth-service
  namespace: auth-service

spec:
  type: NodePort

  selector:
    app: auth-service

  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
      nodePort: 30081