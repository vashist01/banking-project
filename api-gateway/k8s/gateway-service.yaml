apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: banking

spec:
  selector:
    app: api-gateway

  ports:
    - name: http
      port: 8888
      targetPort: 8888
      protocol: TCP

  type: NodePort